# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ─── Messaging (Kafka / Redpanda) ─────────────────────────────────────────────
confluent-kafka>=2.5.0

# ─── Storage Clients ──────────────────────────────────────────────────────────
asyncpg>=0.29.0                         # async PostgreSQL + TimescaleDB
psycopg2-binary>=2.9.9                  # sync PostgreSQL (LangGraph checkpointer)
elasticsearch[async]>=8.15.0,<9.0.0        # must match ES 8.15 server
neo4j>=5.24.0                           # Neo4j driver

# ─── Agent Orchestration ──────────────────────────────────────────────────────
langgraph>=0.2.28
langgraph-checkpoint-postgres>=2.0.1
langchain-groq>=0.2.0
groq>=0.30.0
langchain-ollama>=0.2.0
langchain-core>=0.3.0
langchain>=0.3.0

# ─── ML / Anomaly Detection ───────────────────────────────────────────────────
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
river>=0.21.0                           # online / streaming ML
drain3>=0.9.11                          # streaming log template miner

# ─── Config & Utils ───────────────────────────────────────────────────────────
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
httpx>=0.27.0                           # async HTTP (Ollama health checks etc.)
click>=8.1.7                            # CLI for fault injector
structlog>=24.4.0                       # structured logging

# ─── Serialization ────────────────────────────────────────────────────────────
orjson>=3.10.0                          # fast JSON (used in FastAPI responses)
# ─── OTel Protobuf (for decoding protobuf-encoded OTLP from the collector) ────
opentelemetry-proto>=1.27.0
protobuf>=4.25.0
